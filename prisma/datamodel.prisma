type Currency {
  id: ID! @unique
  name: String! @unique
  nameEng: String @unique
  nominal: Int!
  charCode: String! @unique
  value: Float!
  updatedAt: DateTime!
  createdAt: DateTime!
}

type Heroku {
  id: ID! @unique
  result: String!
  updatedAt: DateTime!
  createdAt: DateTime!
}
